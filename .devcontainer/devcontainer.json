{
    "name": "Python3",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "extensions": [
        "ms-python.python",
        "magicstack.magicpython"
    ],
    "settings": {},
    "remoteUser": "vscode",
    "postCreateCommand": "python3 -m venv env && source env/bin/activate && pip install -r dev-requirements.txt && pip install -r requirements.txt"
}
